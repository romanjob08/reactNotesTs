import React, {FC} from "react";
import {Note} from "../../common";

type PropsType = {
    note: Note
}

export const TableBody: FC<PropsType> = () => {
    return <>
        <div>TBody</div>
    </>
}