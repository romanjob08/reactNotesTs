export interface INotes {
    id: string
    image: string
    name: string
    created: string
    category: string
    content: string
    active: boolean
    archived: boolean
    dates: string
}

